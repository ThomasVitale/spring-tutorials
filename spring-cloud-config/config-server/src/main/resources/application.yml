server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ThomasVitale/spring-tutorials/spring-cloud-config/config-repo
